// std.math — fonctions mathématiques
pub fn clamp(val: f32, min: f32, max: f32) -> f32 {
    if (val < min) { return min; }
    if (val > max) { return max; }
    return val;
}

pub fn lerp(a: f32, b: f32, t: f32) -> f32 {
    return a + (b - a) * t;
}
