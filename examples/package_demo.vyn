// VPM packages demo — serde, collections, neural
import std.io;
import serde;
import collections;
import neural;

fn main() -> i32 {
    io.println("=== VPM Packages ===");

    io.println("serde OK");
    let json = serialize(42);
    io.println(json);

    let mut stack = stack_new();
    stack = stack_push(stack, 10);
    stack = stack_push(stack, 20);
    io.println(stack_pop(stack));

    let y = relu(-1.5);
    io.println(y);

    io.println("All packages working.");
    return 0;
}
